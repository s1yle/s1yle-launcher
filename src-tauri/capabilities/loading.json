{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "loading",
  "description": "No permissions for the loading splash window",
  "windows": ["loading"],
  "permissions": [
    "core:default"
  ]
}
